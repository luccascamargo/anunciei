import { RegisterForm } from "@/components/register-form";

export default function Register() {
  return (
    <div className="container m-auto px-6 h-screen flex items-center justify-center">
      <RegisterForm />
    </div>
  );
}
